# Experiments

Every material experiment starts with a specification in `specs/` and ends with a
report in `reports/`. Raw generated outputs may live under the gitignored
`outputs/` directory; reports must link to or summarize the durable evidence.

Do not edit success criteria after inspecting results. If the protocol changes,
record the deviation in the report and create a new specification when necessary.
